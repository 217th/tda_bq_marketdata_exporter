# BigQuery Configuration
# Copy this file to .env and fill in your values

# Google Cloud Project Configuration
GCP_PROJECT_ID=your-project-id
BQ_DATASET=your_dataset_name
BQ_TABLE=your_table_name

# Path to Google Cloud Service Account key (JSON)
GCP_KEY_PATH=/path/to/your/service-account-key.json

# Application Configuration
SERVICE_NAME=bq-stock-extractor
ENVIRONMENT=development

# Logging Configuration (optional)
LOG_LEVEL=INFO

# Google Cloud Storage Configuration (optional)
# If configured, JSON output will be saved to GCS by default
# Use --output flag to save to local filesystem instead
GCS_BUCKET_NAME=your-bucket-name
GCS_SERVICE_ACCOUNT_KEY=/path/to/gcs-service-account-key.json

# Note: GCS service account should have the following roles:
# - Storage Object Creator (to upload files)
# - Storage Object Viewer (to generate download URLs)
# The bucket should have 'allUsers' with 'Storage Object Viewer' permission for public access

